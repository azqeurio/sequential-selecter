# Core dependencies (required)
PySide6
Pillow
rawpy
pillow-heif
exifread
numpy
opencv-python

# Build tool
pyinstaller

# Optional: AI Denoising (installed at runtime via app UI)
# torch
# torchvision
# spandrel
